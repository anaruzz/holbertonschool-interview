#include <stdlib.h>
#include <stdio.h>

#include "binary_trees.h"

/**
* heap_extract - extracts root of MBH
* @root: root of the node
* Return: value stored
*/
int heap_extract(heap_t **root)
{
heap_t *tmp;
tmp = *root;
if (tmp)
return (0);
return (0);
}
